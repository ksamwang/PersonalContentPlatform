import { useState } from "react";
import { api, MediaSettings } from "../../lib/api";
import { Field, SettingsSection } from "./SettingsSection";

function MediaForm({workspace,value,onChange,canEdit}:{workspace:string;value:MediaSettings;onChange:(v:MediaSettings)=>void;canEdit:boolean}){const[key,setKey]=useState(""),[status,setStatus]=useState("");const name=value.purpose==="ocr"?"OCR Provider":"语音转写 Provider";return <SettingsSection title={name} description={value.purpose==="ocr"?"识别收件箱图片中的文字，兼容多模态 OpenAI API。":"转写收件箱音频，兼容 /audio/transcriptions。"} canEdit={canEdit} onSave={async()=>{try{const saved=await api.saveMedia(workspace,value.purpose,{...value,api_key:key||undefined});onChange(saved);setKey("");setStatus("配置已保存")}catch(error){setStatus(error instanceof Error?error.message:"保存失败")}}}><Field label="Base URL"><input disabled={!canEdit} type="url" value={value.base_url} onChange={e=>onChange({...value,base_url:e.target.value})}/></Field><Field label="API Key" hint={value.api_key_set?`当前：${value.api_key_mask}；留空保留原密钥`:"填写服务密钥"}><input disabled={!canEdit} type="password" value={key} onChange={e=>setKey(e.target.value)}/></Field><Field label="模型"><input disabled={!canEdit} value={value.model} onChange={e=>onChange({...value,model:e.target.value})}/></Field><span aria-live="polite">{status}</span></SettingsSection>}
export function MediaProviderSettings({workspace,values,setValues,canEdit}:{workspace:string;values:MediaSettings[];setValues:(v:MediaSettings[])=>void;canEdit:boolean}){return <>{values.map((value,index)=><MediaForm key={value.purpose} workspace={workspace} value={value} canEdit={canEdit} onChange={next=>setValues(values.map((item,i)=>i===index?next:item))}/>)}</>}
